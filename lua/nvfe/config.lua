-- nvfe.config
--

local config = {
    user_config = {
        dirs = {
            base = "/home/jason/.nvfe/",
            oracle = "oracle",
            log = "log/",
        }

    }
}

return config
